<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context="com.socbox.richard.searchfunction.SearchResults">

    <TextView
        android:id="@+id/results_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/search_results_title_top_margin"
        android:layout_marginBottom="@dimen/search_results_title_bottom_margin"
        android:text="@string/search_results_title_text"
        android:textSize="@dimen/search_results_title_text_size"
        android:gravity="center_horizontal"
        android:textStyle="bold">
    </TextView>


    <ListView
        android:layout_below="@id/results_title"
        android:id="@+id/mainlist"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone">
    </ListView>

    <TextView
        android:id="@+id/noresultstext"
        android:layout_below="@id/results_title"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:text="@string/no_results_text"
        android:textSize="@dimen/no_results_text_size"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:gravity="center"
        android:layout_marginBottom="@dimen/no_results_bottom_margin"
        android:visibility="visible">
    </TextView>

</RelativeLayout>
